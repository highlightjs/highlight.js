@name SyntaxTest
@persist Array:table Num:number
@model models/suckers/punched_001.mdl
#[ This E2 is to test Syntax highlighting for my E2 Editor
   This is block comment]# Test=array(vec4(0b101,
    0xDEADBEEF,-2.075,3e12):toString())
#include "test.rofl"
function array string:booo_m(Number:string,Rub_bish:wirelink){
    local I=0
    while(1){
        if(I==1){
            continue,I++}
        elseif(I==2){
            break}
        else{
            print(_HUD_PRINTTALK,"Multiline string:
            bla bla\\\" bla
            blub\\\\\"  This is still a string
            Pi=\\\\"+_PI),I++ # This isn't
        }
    },return This:explode(Number)
}
Test:merge(array("lol,rofl substrings \"lol\" \\",
    "wtf?\\\"triple,escapes?\\\" What a mad lad it's 4 \\\\"))
foreach(N,String:string=Test){
    Test[N,string]=String:booo_m(",",entity():wirelink())[1,string]
}#lol #[rofl]#]#Nope, still comment, not a blockcomment here :D
#ifdef need:toFill(Lines:here)
   toTest(Text,overflow())
#endif
#that ^ is off screen first
