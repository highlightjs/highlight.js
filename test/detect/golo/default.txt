module tests

import gololang.Async

struct human = {
    firstName, lastName
}
augment human {
    function initialize = |this| {
        println("I'm an initializer")
        return this
    }
    function hello = |this| {
        println("hello")
        var z = null orIfNull true
    }
}

augmentation = { 
    function pouet = |arg| ->
        """
        hello
        """
}

# this is a comment
function main = |args| {
    let bob = human("Bob", "Morane"): initialize()
    foreach item in bob { println(item) }
    let sam =DynamicObject(): name("sam")
    var olivia = Observable("olivia")
    let z = array[4,5,6,7]
}