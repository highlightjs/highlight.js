interface myInterface {
  name: string;
}

interface lowercase {
  isLowercase: boolean;
}

interface thisInterface extends OtherName {
  length: number;
  width: number;
}

interface newline extends ALLCAPS
{
  isSpace: boolean;
}
